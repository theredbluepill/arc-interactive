# Optional: only needed for API-backed runs. Local play: omit this file or leave the key empty.
#
# ARC Leaderboard: https://three.arcprize.org
# Use with:  uv run python run_game.py --online ...   (or --competition)
ARC_API_KEY=
#
# ARC_GAME_ID is optional — only if you omit --game (overrides default stem: co01 local environments, ls20 --online).
